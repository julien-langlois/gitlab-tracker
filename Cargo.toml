[workspace]
resolver = "3"
members = [
    "gitlab-tracker",
    "gitlab-tracker-notify",
    "gitlab-tracker-core",
    "gitlab-tracker-redmine",
]

[workspace.package]
version = "0.3.6"
authors = ["Julien Langlois <julien@julienlanglois.fr>"]
edition = "2021"
license = "MIT"
repository = "https://github.com/julien-langlois/gitlab-tracker"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
